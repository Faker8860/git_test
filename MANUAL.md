# 量化交易系统 — 使用手册

## 快速开始

### 1. 部署（管理员）

```bash
# 克隆代码
git clone git@github.com:GGhuazi/trading-system.git
cd trading-system

# 安装依赖
pip install ccxt pandas numpy python-dotenv fastapi uvicorn

# 配置环境变量（复制模板修改）
cp .env.example .env
# 编辑 .env，填入 ADMIN_PASSWORD 和默认代理

# 启动服务
python web_ui.py
# 访问 http://你的服务器IP:8000
```

### 2. 注册账号

打开浏览器访问 `http://服务器IP:8000`：

1. 点击「**立即注册**」
2. 输入用户名（1-30字符）和密码（≥4位）
3. 点击注册，成功后自动跳转登录

### 3. 配置币安 API

登录后进入仪表盘 → 看到提示「请先配置币安 API 密钥」→ 点击「**前往设置**」：

| 字段 | 说明 | 示例 |
|------|------|------|
| API Key | 币安 API Key | `EoSNCVr0awot...` |
| Secret Key | 币安 Secret Key | `0WbI7Lw7FlQQ...` |
| 代理 URL | VPN/Clash 代理地址 | `http://127.0.0.1:7897` |

> ⚠️ API Key 需要在币安创建时勾选「合约交易」权限

点击「**保存 API 密钥**」，2 秒后自动刷新，右上角显示「● 在线」即配置成功。

### 4. 配置交易策略

左侧菜单 → 「**策略配置**」：

- **策略类型**: TEMA（趋势跟随）或 VOLTY（波动性突破）
- **交易币种**: ETHUSDT, BTCUSDT 等
- **K线周期**: 1/3/5/15/30/60 分钟
- **仓位比例**: 账户余额的百分比
- **杠杆倍数**: 1-125x
- **止损比例**: 如 5%
- **交易方向**: 做多 / 做空 / 双向

点击「**保存并应用**」，自动重启机器人。

### 5. 启动交易机器人

策略配置页 → 点击「**启动策略**」按钮。

或在仪表盘查看状态：右上角显示「● 在线 / ● 离线」。

## 功能导航

| 页面 | 功能 |
|------|------|
| 仪表盘 | 实时持仓、余额、盈亏曲线、行情 |
| 策略配置 | 策略参数、启动/停止机器人 |
| 数据监控 | 全市场行情排名 |
| 资产分析 | 胜率、盈亏比、交易对偏好 |
| 系统日志 | 策略运行日志 |
| 设置中心 | API 密钥、代理配置 |

## 环境变量参考

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `ADMIN_PASSWORD` | 管理员密码 | 无（不设则无法使用管理功能） |
| `PROXY_URL` | 默认代理地址 | 空 |
| `SERVER_HOST` | 服务器 IP（启动日志显示） | localhost |
| `UI_PORT` | Web 端口 | 8000 |
| `LEVERAGE` | 默认杠杆 | 3 |
| `STOP_LOSS_PCT` | 默认止损比例 | 5 |

## 常见问题

**Q: 右上角显示「币安离线」？**
A: 检查代理 URL 是否正确，VPN 是否开启。

**Q: 忘记密码？**
A: 登录后可在设置中修改密码，或联系管理员重置。

**Q: 怎么同时交易多个币种？**
A: 策略配置页 → 点击「添加策略」，每个币种独立配置。

**Q: 预设账户？**
A: `A8` / `13590703676`（首次启动自动创建）

## 目录结构

```
trading-system/
├── web_ui.py          # Web 主程序
├── strategy_bot.py    # 策略机器人
├── bot_server.py      # TradingView Webhook 桥接器
├── db.py              # 用户数据库
├── chart.js           # 图表库（本地）
├── accounts.db        # 用户数据库文件
├── .env               # 全局配置
└── user_data/         # 用户数据目录
    └── <用户名>/
        ├── .env           # 用户专属币安密钥
        ├── trades.json    # 交易记录
        ├── strategies.json# 策略配置
        └── watchlist.json # 自选列表
```
